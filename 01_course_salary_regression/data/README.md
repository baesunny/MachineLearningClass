# 데이터 안내

수업에서 제공된 **임금(Salary) 회귀** 예제 CSV는 저작권·참가 서약으로 저장소에 포함하지 않는다.

로컬 재현 시 아래 파일을 이 폴더에 배치한다.

- `X_train.csv`, `y_train.csv`, `X_test.csv` (cp949 인코딩)

노트북은 `notebooks/`에서 실행하며, 경로는 `../data/`를 기준으로 한다.
