# KML Challenge 2023F 데이터 안내

[KML Challenge 2023F](https://www.kaggle.com/competitions/kml-challenge-2023f)의 `train.csv`, `test.csv`는 대회 규정상 저장소에 포함하지 않는다.

로컬·Kaggle 재현 시 이 폴더에 두 파일을 배치한다.

- `train.csv`, `test.csv` (cp949)

노트북은 `notebooks/` 또는 `notebooks/experiments/`에서 실행하며, 경로는 `../data/`를 기준으로 한다.
